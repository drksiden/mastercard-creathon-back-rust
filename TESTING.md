# 🧪 Тестирование API

## Быстрая проверка работы

### 1. Health Check

Проверьте, что сервер работает:

```bash
curl http://localhost:3000/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-11-15T13:17:52Z",
  "database": "connected",
  "llm": "gemini"
}
```

### 2. Простой запрос

Проверьте генерацию SQL:

```bash
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{"question": "Сколько всего транзакций?"}'
```

Ожидаемый ответ:
```json
{
  "question": "Сколько всего транзакций?",
  "sql": "SELECT COUNT(*) as total FROM transactions;",
  "data": [
    {
      "total": 5000
    }
  ],
  "execution_time_ms": 12,
  "row_count": 1
}
```

### 3. Использование тестового скрипта

Запустите автоматический тест:

```bash
./test_api.sh
```

Или вручную:

```bash
# Health check
curl http://localhost:3000/api/health | jq '.'

# Простой запрос
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{"question": "Сколько транзакций было сегодня?"}' | jq '.'

# Запрос с фильтрацией
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{"question": "Топ 5 мерчантов по объему транзакций"}' | jq '.'
```

## Примеры вопросов для тестирования

### Простые запросы:
- "Сколько всего транзакций?"
- "Сколько транзакций было сегодня?"
- "Сколько транзакций было вчера?"

### Запросы с фильтрацией:
- "Топ 5 мерчантов по объему транзакций"
- "Транзакции в городе Алматы"
- "Транзакции по картам Halyk Bank"

### Запросы по категориям:
- "Объем транзакций по категориям MCC"
- "Транзакции в категории 'Dining & Restaurants'"
- "Топ категории по количеству транзакций"

### Запросы по типам транзакций:
- "Количество POS транзакций"
- "Сравнение POS и ECOM транзакций"
- "P2P транзакции (входящие и исходящие)"

### Запросы по валютам:
- "Транзакции в валюте KZT"
- "Транзакции в валюте USD"
- "Сравнение транзакций по валютам"

## Проверка логов

Сервер выводит логи в консоль. Обратите внимание на:

1. **Успешная инициализация:**
   ```
   INFO: Configuration loaded
   INFO: Database connected
   INFO: Tables already exist, skipping migrations
   INFO: LLM client initialized: gemini
   INFO: LLM ready!
   INFO: 🚀 Server running on http://0.0.0.0:3000
   ```

2. **Ошибки Gemini API:**
   - Если видите `LLM warm-up failed` - это нормально, сервер продолжит работу
   - Если видите ошибки при запросах - проверьте `GEMINI_API_KEY`

3. **Ошибки базы данных:**
   - Проверьте `DATABASE_URL` в `.env`
   - Убедитесь, что PostgreSQL запущен

## Troubleshooting

### Проблема: "Connection refused"
**Решение:** Убедитесь, что сервер запущен на порту 3000

### Проблема: "Gemini API error"
**Решение:** 
- Проверьте `GEMINI_API_KEY` в `.env`
- Убедитесь, что ключ действителен
- Проверьте интернет-соединение

### Проблема: "Database connection failed"
**Решение:**
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа к базе данных

### Проблема: "Empty response from Gemini API"
**Решение:**
- Попробуйте другой вопрос
- Проверьте логи для деталей ошибки
- Убедитесь, что Gemini API доступен

## Использование с jq (для красивого вывода)

Установите `jq` для форматирования JSON:

```bash
# Ubuntu/Debian
sudo apt-get install jq

# Arch Linux
sudo pacman -S jq

# macOS
brew install jq
```

Затем используйте:
```bash
curl http://localhost:3000/api/health | jq '.'
```

