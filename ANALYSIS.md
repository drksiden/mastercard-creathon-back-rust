# 📊 Анализ данных и кэширование

## Обзор

Система теперь включает два ключевых компонента:

1. **SQL-генератор с оптимизацией** - автоматически генерирует агрегированные запросы
2. **LLM-анализ результатов** - преобразует данные в понятный текст с инсайтами
3. **Кэширование** - ускоряет повторные запросы

## 🚀 Оптимизация SQL-запросов

### Принудительная агрегация

Система автоматически оптимизирует SQL-запросы:

- ✅ **Всегда использует агрегацию** (SUM, COUNT, AVG, GROUP BY) для больших датасетов
- ✅ **Всегда включает WHERE с фильтром по времени** (если не запрашиваются общие итоги)
- ✅ **Всегда использует LIMIT** при возврате отдельных строк (макс. 100, предпочтительно 10-20)
- ✅ **Никогда не возвращает сырые транзакции** без агрегации или LIMIT

### Примеры оптимизированных запросов

**Вместо:**
```sql
SELECT * FROM transactions;  -- ❌ Вернет миллионы строк
```

**Система сгенерирует:**
```sql
SELECT COUNT(*) as total_transactions 
FROM transactions 
WHERE transaction_timestamp >= CURRENT_DATE - INTERVAL '7 days'
LIMIT 1;  -- ✅ Агрегация + фильтр + LIMIT
```

**Или:**
```sql
SELECT mcc_category, SUM(transaction_amount_kzt) as total, COUNT(*) as count
FROM transactions 
WHERE transaction_timestamp >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY mcc_category
ORDER BY total DESC
LIMIT 10;  -- ✅ Группировка + агрегация + LIMIT
```

## 💾 Кэширование

### Стратегия кэширования

Система использует **in-memory кэш** с автоматическим TTL:

- **Оперативные запросы** (с `CURRENT_DATE`, `today`, `last`): **5 минут**
- **Исторические данные**: **30 минут**

### Использование кэша

Кэширование включено по умолчанию. Можно отключить:

```json
{
  "question": "Сколько транзакций сегодня?",
  "use_cache": false  // Отключить кэш
}
```

### Ключ кэша

Кэш использует хеш SQL-запроса как ключ. Это означает:
- ✅ Одинаковые запросы → мгновенный ответ из кэша
- ✅ Разные формулировки вопроса → разные SQL → разные ключи кэша

## 📈 LLM-анализ результатов

### Включение анализа

Для получения структурированного анализа с инсайтами:

```json
{
  "question": "Какие категории MCC самые популярные?",
  "include_analysis": true
}
```

### Структура ответа

```json
{
  "question": "Какие категории MCC самые популярные?",
  "sql": "SELECT mcc_category, COUNT(*) as count FROM transactions GROUP BY mcc_category ORDER BY count DESC LIMIT 10;",
  "data": [...],
  "analysis": {
    "headline": "Топ-10 категорий MCC по количеству транзакций",
    "insights": [
      {
        "title": "Доминирование ресторанов",
        "description": "Категория 'Dining & Restaurants' занимает первое место с 35% всех транзакций",
        "significance": "High"
      }
    ],
    "explanation": "Анализ показывает, что наибольшее количество транзакций приходится на категорию 'Dining & Restaurants'...",
    "suggested_questions": [
      "Показать динамику транзакций по ресторанам за последний месяц",
      "Сравнить средний чек по категориям"
    ],
    "chart_type": "Bar",
    "data": [...]
  },
  "cached": false
}
```

### Компоненты анализа

1. **Headline** - краткий ответ на вопрос (1-2 предложения)
2. **Insights** - ключевые находки с уровнем значимости (High/Medium/Low)
3. **Explanation** - подробное объяснение результатов (2-3 абзаца)
4. **Suggested Questions** - предложения следующих вопросов
5. **Chart Type** - рекомендация типа диаграммы (Bar/Line/Pie/Table/Trend)

## 🔄 Полный цикл работы

### Пример 1: Простой запрос с анализом

```bash
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Сколько транзакций было сегодня?",
    "include_analysis": true
  }'
```

**Ответ:**
```json
{
  "question": "Сколько транзакций было сегодня?",
  "sql": "SELECT COUNT(*) as total FROM transactions WHERE DATE(transaction_timestamp) = CURRENT_DATE;",
  "data": [{"total": 1523}],
  "execution_time_ms": 45,
  "row_count": 1,
  "analysis": {
    "headline": "Сегодня было обработано 1,523 транзакции",
    "insights": [
      {
        "title": "Активность в пределах нормы",
        "description": "Количество транзакций соответствует среднему дневному уровню",
        "significance": "Medium"
      }
    ],
    "explanation": "За сегодняшний день система обработала 1,523 транзакции...",
    "suggested_questions": [
      "Показать распределение транзакций по часам",
      "Сравнить с вчерашним днем"
    ],
    "chart_type": "Table"
  },
  "cached": false
}
```

### Пример 2: Запрос с кэшированием

```bash
# Первый запрос - выполняется в БД
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Топ-5 городов по объему транзакций",
    "use_cache": true
  }'

# Второй запрос (тот же SQL) - из кэша
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Топ-5 городов по объему транзакций",
    "use_cache": true
  }'
```

**Первый ответ:**
```json
{
  "cached": false,
  "execution_time_ms": 234
}
```

**Второй ответ:**
```json
{
  "cached": true,
  "execution_time_ms": 1  // Мгновенный ответ из кэша
}
```

## 🎯 Рекомендации по использованию

### Когда использовать анализ

✅ **Используйте анализ для:**
- Сложных аналитических запросов
- Презентаций и отчетов
- Объяснения данных пользователям
- Автоматической генерации инсайтов

❌ **Не используйте анализ для:**
- Простых запросов (COUNT, SUM)
- Высокочастотных запросов (API интеграции)
- Когда нужны только сырые данные

### Когда использовать кэш

✅ **Используйте кэш для:**
- Повторяющихся запросов
- Дашбордов и отчетов
- Исторических данных

❌ **Отключите кэш для:**
- Реал-тайм данных (если нужна актуальность)
- Тестирования и отладки
- Когда данные меняются часто

## 🔧 Конфигурация

### Переменные окружения

```bash
# LLM для анализа (использует тот же провайдер, что и SQL-генерация)
LLM_PROVIDER=gemini
GEMINI_API_KEY=your_key
GEMINI_MODEL=gemini-2.5-flash

# Кэш настраивается автоматически
# TTL: 5 минут для оперативных, 30 минут для исторических
```

## 📊 Производительность

### Без кэша
- SQL-генерация: ~200-500ms
- Выполнение запроса: ~50-500ms (зависит от сложности)
- Анализ (если включен): ~1-3s
- **Итого: ~1-4s**

### С кэшем (cache hit)
- SQL-генерация: ~200-500ms
- Выполнение запроса: **~1ms** (из кэша)
- Анализ (если включен): ~1-3s
- **Итого: ~1.2-3.5s**

### С кэшем и без анализа
- SQL-генерация: ~200-500ms
- Выполнение запроса: **~1ms** (из кэша)
- **Итого: ~200-500ms**

## 🚀 Будущие улучшения

- [ ] Redis для распределенного кэширования
- [ ] Materialized Views для предварительной агрегации
- [ ] Streaming ответов для больших датасетов
- [ ] Кэширование анализа отдельно от данных
- [ ] Адаптивный TTL на основе частоты запросов

