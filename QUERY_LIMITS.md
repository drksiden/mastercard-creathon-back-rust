# 🛡️ Ограничения и правила запросов

## Почему запрос "Напиши первые 200к записей" отклонен?

Система **автоматически отклоняет** запросы, которые:
- ❌ Просят вернуть слишком много сырых данных (без агрегации)
- ❌ Могут перегрузить базу данных
- ❌ Нарушают правила оптимизации

### Правила оптимизации

Система **всегда** применяет:
1. ✅ **Агрегацию** (SUM, COUNT, AVG, GROUP BY) для больших датасетов
2. ✅ **LIMIT** при возврате отдельных строк (макс. 100, предпочтительно 10-20)
3. ✅ **WHERE с фильтром по времени** (если не запрашиваются общие итоги)

## ✅ Правильные способы запросить данные

### Вместо: "Напиши первые 200к записей"

**Используйте:**

1. **Агрегированные данные:**
   ```
   "Показать топ-20 транзакций по сумме"
   "Топ-10 категорий MCC"
   "Средний чек по типам транзакций"
   ```

2. **С ограничением:**
   ```
   "Показать первые 20 транзакций"
   "Последние 50 транзакций типа POS"
   ```

3. **С группировкой:**
   ```
   "Количество транзакций по категориям"
   "Сумма транзакций по дням за последнюю неделю"
   ```

## 📊 Примеры правильных запросов

### ✅ Хорошие запросы:

```bash
# Агрегированные данные
"Топ-10 категорий MCC по количеству транзакций"
"Сколько транзакций каждого типа?"
"Средний чек по городам"

# С ограничением
"Показать первые 20 транзакций"
"Последние 50 транзакций"

# Временные ряды
"Динамика транзакций по дням за последнюю неделю"
"Транзакции по месяцам за год"
```

### ❌ Плохие запросы (будут отклонены):

```bash
# Слишком много данных
"Напиши первые 200к записей"
"Покажи все транзакции"
"Выведи все данные из таблицы"

# Без агрегации
"Покажи все транзакции за год"  # Вместо: "Количество транзакций по месяцам"
```

## 🔍 Как система определяет плохие запросы?

LLM анализирует запрос и проверяет:
- Просит ли запрос сырые данные без агрегации?
- Указывает ли запрос слишком большое количество записей?
- Можно ли запрос оптимизировать через GROUP BY/LIMIT?

Если да → генерирует SQL с ошибкой:
```sql
SELECT 'Невозможно сгенерировать SQL для данного запроса.' as error;
```

## 💡 Рекомендации

### Для больших объемов данных:

1. **Используйте агрегацию:**
   ```
   ❌ "Покажи все транзакции"
   ✅ "Количество транзакций по категориям"
   ```

2. **Используйте группировку:**
   ```
   ❌ "Все транзакции за месяц"
   ✅ "Сумма транзакций по дням за месяц"
   ```

3. **Используйте TOP N:**
   ```
   ❌ "Покажи все города"
   ✅ "Топ-10 городов по объему транзакций"
   ```

## 🎯 Примеры преобразования запросов

| ❌ Плохой запрос | ✅ Хороший запрос |
|-----------------|-------------------|
| "Напиши первые 200к записей" | "Топ-20 транзакций по сумме" |
| "Покажи все транзакции" | "Количество транзакций по типам" |
| "Выведи все данные" | "Сводка по категориям MCC" |
| "Все транзакции за год" | "Динамика транзакций по месяцам" |

## 📝 Итог

Система **правильно** отклоняет запросы, которые:
- Могут перегрузить БД
- Вернут слишком много данных
- Не оптимизированы

Это **защита**, а не ошибка! Используйте агрегацию и ограничения для получения нужных данных.

