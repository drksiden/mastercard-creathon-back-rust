# 🚀 Пошаговая установка и настройка

## Шаг 1: Установка Ollama

Ollama - это инструмент для запуска LLM моделей локально на вашем компьютере.

### Linux (Arch/Ubuntu/Debian)

```bash
# Скачайте и установите Ollama
curl -fsSL https://ollama.com/install.sh | sh
```

Или вручную:
```bash
# Для Arch Linux (AUR)
yay -S ollama
# или
paru -S ollama

# Для других дистрибутивов - скачайте с https://ollama.com/download
```

### Проверка установки

```bash
ollama --version
```

## Шаг 2: Запуск Ollama сервера

Ollama должен работать как сервис в фоновом режиме:

```bash
# Запустите Ollama сервер
ollama serve
```

Оставьте этот терминал открытым. Сервер будет работать на `http://localhost:11434`

**Альтернатива:** Запуск как системный сервис (рекомендуется):

```bash
# Создайте сервис (systemd)
sudo systemctl enable ollama
sudo systemctl start ollama

# Проверьте статус
sudo systemctl status ollama
```

## Шаг 3: Загрузка модели

Вам нужно загрузить одну из моделей. Рекомендуемые варианты:

### Вариант 1: Llama 2 (7B) - легкая, быстрая (~4GB)

```bash
ollama pull llama2
```

**Плюсы:**
- Быстрая загрузка
- Меньше места на диске
- Достаточно для простых SQL запросов

**Минусы:**
- Может генерировать менее точный SQL для сложных запросов

### Вариант 2: Mixtral 8x7B - более мощная (~26GB)

```bash
ollama pull mixtral:8x7b-instruct
```

**Плюсы:**
- Более точная генерация SQL
- Лучше понимает сложные запросы

**Минусы:**
- Требует больше места на диске
- Медленнее работает

### Вариант 3: Mistral (7B) - баланс (~4GB)

```bash
ollama pull mistral
```

**Плюсы:**
- Хороший баланс между размером и качеством
- Быстрее чем Mixtral, но лучше чем Llama 2

### Проверка загруженных моделей

```bash
# Список всех загруженных моделей
ollama list

# Тест модели
ollama run llama2 "Generate SQL query to count all rows in a table called transactions"
```

## Шаг 4: Настройка .env файла

Создайте файл `.env` в корне проекта:

```bash
cd /home/danil/dev/hackaton/mastercard-creathon-back-rust
nano .env  # или используйте любой редактор
```

Вставьте следующее (выберите модель, которую загрузили):

```env
# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/payment_analytics

# LLM Configuration
LLM_PROVIDER=ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2  # или mistral, или mixtral:8x7b-instruct

# Server
HOST=0.0.0.0
PORT=3000

# Logging
RUST_LOG=info,payment_analytics_backend=debug
```

**Важно:** Укажите в `OLLAMA_MODEL` ту модель, которую вы загрузили на шаге 3.

## Шаг 5: Настройка PostgreSQL

### Установка PostgreSQL

```bash
# Arch Linux
sudo pacman -S postgresql

# Ubuntu/Debian
sudo apt-get install postgresql postgresql-contrib
```

### Запуск PostgreSQL

```bash
# Запустите PostgreSQL
sudo systemctl start postgresql
sudo systemctl enable postgresql  # автозапуск при загрузке
```

### Создание базы данных

```bash
# Войдите в PostgreSQL как суперпользователь
sudo -u postgres psql

# В консоли PostgreSQL выполните:
CREATE DATABASE payment_analytics;
CREATE USER postgres WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE payment_analytics TO postgres;
\q
```

**Или одной командой:**

```bash
sudo -u postgres psql -c "CREATE DATABASE payment_analytics;"
sudo -u postgres psql -c "CREATE USER postgres WITH PASSWORD 'password';"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE payment_analytics TO postgres;"
```

## Шаг 6: Запуск приложения

```bash
cd /home/danil/dev/hackaton/mastercard-creathon-back-rust

# Запустите сервер
cargo run
```

Вы должны увидеть:
```
Configuration loaded
Database connected
Migrations completed
LLM client initialized: ollama
Warming up LLM...
LLM ready!
🚀 Server running on http://0.0.0.0:3000
```

## Шаг 7: Проверка работы

### Проверка health endpoint

В другом терминале:

```bash
curl http://localhost:3000/api/health
```

Должен вернуться JSON с `"status": "ok"`

### Тестовый запрос

```bash
curl -X POST http://localhost:3000/api/query \
  -H "Content-Type: application/json" \
  -d '{"question": "Сколько всего транзакций?"}'
```

## 🔧 Troubleshooting

### Проблема: "Connection refused" при подключении к Ollama

**Решение:**
```bash
# Проверьте, что Ollama запущен
curl http://localhost:11434/api/tags

# Если не работает, запустите вручную
ollama serve
```

### Проблема: "Model not found"

**Решение:**
```bash
# Проверьте список моделей
ollama list

# Если модели нет, загрузите её
ollama pull llama2  # или другую модель

# Проверьте название модели в .env файле
```

### Проблема: PostgreSQL не запускается

**Решение:**
```bash
# Проверьте статус
sudo systemctl status postgresql

# Если не запущен, запустите
sudo systemctl start postgresql

# Проверьте логи
sudo journalctl -u postgresql -n 50
```

### Проблема: "Database connection failed"

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env`
3. Проверьте, что база данных создана:
   ```bash
   sudo -u postgres psql -l | grep payment_analytics
   ```

### Проблема: LLM генерирует неправильный SQL

**Решение:**
- Попробуйте более мощную модель (mistral или mixtral)
- Уточните вопрос
- Проверьте логи для просмотра сгенерированного SQL

## 📊 Рекомендации по выбору модели

| Модель | Размер | Скорость | Качество | Рекомендация |
|--------|--------|----------|----------|--------------|
| llama2 | ~4GB | ⚡⚡⚡ | ⭐⭐ | Для быстрого старта |
| mistral | ~4GB | ⚡⚡ | ⭐⭐⭐ | Оптимальный выбор |
| mixtral:8x7b | ~26GB | ⚡ | ⭐⭐⭐⭐ | Для продакшена |

## 🎯 Быстрый старт (краткая версия)

```bash
# 1. Установите Ollama
curl -fsSL https://ollama.com/install.sh | sh

# 2. Запустите Ollama
ollama serve  # в отдельном терминале

# 3. Загрузите модель
ollama pull llama2

# 4. Настройте .env (см. Шаг 4)

# 5. Настройте PostgreSQL (см. Шаг 5)

# 6. Запустите приложение
cargo run
```

## 💡 Альтернатива: Использование OpenAI (без локальной установки)

Если не хотите устанавливать Ollama локально, можно использовать OpenAI API:

1. Получите API ключ на https://platform.openai.com/api-keys
2. В `.env` укажите:
   ```env
   LLM_PROVIDER=openai
   OPENAI_API_KEY=sk-your-key-here
   ```

**Примечание:** 
- OpenAI API платный, но не требует установки локально
- Благодаря `rig-core`, переключение между провайдерами происходит автоматически
- Код остается тем же, меняется только конфигурация

