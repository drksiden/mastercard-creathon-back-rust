# 📋 Скрипты для настройки базы данных

## 🚀 Быстрый старт

### Автоматическая настройка (рекомендуется)

```bash
# Использование с параметрами по умолчанию
./scripts/setup_database.sh

# Или с указанием параметров
./scripts/setup_database.sh payment_analytics postgres postgres localhost 5432
```

**Параметры:**
- `DB_NAME` - имя базы данных (по умолчанию: `payment_analytics`)
- `DB_USER` - пользователь PostgreSQL (по умолчанию: `postgres`)
- `DB_PASSWORD` - пароль пользователя (по умолчанию: `postgres`)
- `DB_HOST` - хост PostgreSQL (по умолчанию: `localhost`)
- `DB_PORT` - порт PostgreSQL (по умолчанию: `5432`)

### Ручная настройка

#### 1. Создание базы данных

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE payment_analytics;

# Выйдите из psql
\q
```

#### 2. Применение миграций

```bash
# Примените SQL скрипт для создания таблиц
psql -U postgres -d payment_analytics -f scripts/create_database.sql
```

#### 3. Заполнение тестовыми данными

```bash
# Заполните базу тестовыми данными (5000 транзакций)
psql -U postgres -d payment_analytics -f scripts/insert_test_data.sql
```

Или используйте переменную окружения для пароля:

```bash
export PGPASSWORD=your_password
psql -U postgres -d payment_analytics -f scripts/insert_test_data.sql
```

## 📊 Что создается

### Таблицы

1. **transactions** - основная таблица транзакций
   - Все поля из схемы базы данных
   - Индексы для оптимизации запросов

2. **query_audit_log** - лог всех запросов для аудита
   - Хранит историю всех SQL-запросов
   - Время выполнения и результаты

### Тестовые данные

Скрипт генерирует **5000 транзакций** с:
- Случайными датами за последние 2 года
- Различными типами транзакций (POS, ATM_WITHDRAWAL, P2P_IN, P2P_OUT, и т.д.)
- Различными категориями MCC
- Различными валютами (KZT, USD, EUR, и т.д.)
- Различными странами
- Различными банками-эмитентами
- Различными способами оплаты (Contactless, QR_Code, и т.д.)

## 🔍 Проверка

После выполнения скриптов проверьте данные:

```bash
psql -U postgres -d payment_analytics

# Количество транзакций
SELECT COUNT(*) FROM transactions;

# Примеры транзакций
SELECT * FROM transactions LIMIT 5;

# Статистика по типам транзакций
SELECT transaction_type, COUNT(*) as count 
FROM transactions 
GROUP BY transaction_type 
ORDER BY count DESC;

# Статистика по валютам
SELECT transaction_currency, COUNT(*) as count, 
       SUM(transaction_amount_kzt) as total_kzt
FROM transactions 
GROUP BY transaction_currency 
ORDER BY count DESC;
```

## ➕ Добавление дополнительных транзакций

Если нужно добавить больше транзакций к существующим:

```bash
# Добавить 999500 транзакций (чтобы получить 1,000,000 всего)
./scripts/add_more_transactions.sh 999500

# Или с указанием параметров
./scripts/add_more_transactions.sh 999500 payment_analytics postgres postgres localhost 5432
```

Или вручную через SQL:

```bash
# Добавить 100000 транзакций
psql -U postgres -d payment_analytics -v transaction_count=100000 -f scripts/insert_test_data.sql
```

**Примечание:** Скрипт автоматически определяет текущее количество транзакций и продолжает нумерацию с нужного ID.

## 🗑️ Очистка данных

Если нужно пересоздать данные:

```bash
psql -U postgres -d payment_analytics -c "TRUNCATE TABLE transactions CASCADE;"
psql -U postgres -d payment_analytics -f scripts/insert_test_data.sql
```

## 📝 Использование в приложении

После настройки базы данных добавьте в `.env`:

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/payment_analytics
```

Или используйте формат с параметрами:

```env
DATABASE_URL=postgresql://user:password@host:port/dbname
```

## ⚠️ Важные замечания

1. **Безопасность**: Не используйте пароль `postgres` в продакшене
2. **Права доступа**: Убедитесь, что пользователь имеет права на создание базы данных
3. **Существующие данные**: Скрипт не перезапишет существующие данные без подтверждения
4. **Производительность**: Генерация 5000 транзакций может занять несколько секунд

